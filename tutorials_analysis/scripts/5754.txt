meta version "v2.2,js,ctx";
meta name "Dice tutorial";
meta icon "Cube";
meta color "#ffff2800";
meta rootId "aaNMn6kuus2ATa3dig6qpVYb";
meta platform "current";
// #docs #tutorials

action main() {
  // {template:bqcz}
  // In this tutorial, you will learn how to create an app to simulate the rolling of a dice.
  // {box:screen}
  // {pic:3D Dice:5x5}
  // {/box}
  // {box:print}
  // * open a browser and go to [http://touchdevelop.com/](http://touchdevelop.com/)
  // * login with your Microsoft, Facebook, Google or Yahoo account
  // * tap on `[Create Script]`
  // {pic:create script button}
  // * tap on `[blank]` template
  // * give your script a name and tap on **create**
  // {/box}
  // 
  // ### Let's get started
  // 
  // First we need to create the **game board**.
  // A game board is a 2D game engine which allows you to create simple sprite-based games.
  // Before creating the game board, you need to decide whether the game will be played in landscape or portrait mode. You also need to decide the width and height in pixels; the game board will be scaled based on the user screen.
  // Letaˆ™s create a game board for a portrait game (480 pixels by 800 pixels)
  // 
  // ### Setting up the game board
  // 
  // * tap on `[a–·main]` under `[code]`
  // * tap on `do nothing`
  // * tap on `[var]`
  // * rename the variable from **x** to **board**
  // * tap on `[media]`
  // * tap on `[create portrait board]`
  // {box:hint}
  // If you canaˆ™t see the **create portrait board** button, click on the blue `a–·a–·` arrow button to cycle through all the available options. Alternatively, if you can't find the button you need, tap on `[Search APIs]` and type in what you are looking for.
  // {/box}
  // Next we need to store **board** in a global variable.
  // * promote `board` to a global variable by tapping on `[promote to data]`
  // {box:example}
  // * tap on `board` so that it is highlighted in green (see below)
  // {pic:varboard:1x3}
  // * tap on `[promote to data]`
  // {/box}
  dataa†’board := mediaa†’create_portrait_board(480, 800);
  // A board remains invisible until it is posted to the wall.
  // * tap on `[+]` to add a new line
  // {pic:addnewlineicon:1x2}
  // * tap on `[a—?data]`
  // * tap on `[board]`
  // * tap on `[post to wall]`
  dataa†’boarda†’post_to_wall;
  // 
  // ### Adding the dice sprites
  // 
  // The next step is to add sprites to our script to represent each side of our dice.
  // There are four types of sprite: `elipse`, `rectangle`, `text` and `picture`
  // We are going to use an image to represent each side of the dice. You can either upload a picture of your own or reuse a picture that another user has already uploaded. For this tutorial, we will use an image that a user has already loaded.
  // * tap on `[add new action, event, ...]`
  // {pic:add new action screenshot:20x20}
  // * tap on `[pic : Picture]`
  // * scroll down and tap on `[search online art pictures]`
  // {pic:searchonlineart:2x9}
  // * type **dice** in the search bar.
  // {pic:search dice}
  // * tap on **Dice 1**
  // The image will now appear in your art library:
  // {box:example}
  // {pic:diceart}
  // {/box}
  // * repeat the above steps for dice sides **2, 3, 4, 5** and **6**. **Hint:** You will need to `[add new action, event, ...]` for each dice.
  // 
  // ### Rolling the dice
  // 
  // Next we need to instruct the computer to generate a random number between 1 and 6.
  // 
  // * tap on `[a–·main]` under `[code]`
  // * tap on `[+]` to add a new line
  // We will use a variable to store the random number.
  // * tap on `[var]`
  // * rename the variable to **dice roll**.
  // * tap on `[math]`
  // * tap on `[random]`
  // Your code should look like this: `var dice roll := math -> random(2)`
  // {box:hint}
  // **Remember:** Click on the blue `a–·a–·` arrow button to cycle through all the available options. Alternatively, if you can't find the button you need, tap on `[Search APIs]` and type in what you are looking for.
  // {/box}
  // * replace the 2 (inside the parenthesis) with a **6**.
  // Your code should now look like this: `var dice roll := math -> random(6)`
  // Due to the way in which TouchDevelop works, the code will generate a number between 0 and 5 *NOT* 1 and 6.
  // In order to rectify this, we need to add a 1 to the randomly generated number i.e. a 0 will become a 1 and 5 will become 6 etc.
  // * tap on the end of the line beginning:  `var dice roll := math`
  // * tap on `[+]` (Adds numbers button)
  // * type in **1**
  $dice_roll := matha†’random(6) + 1;
  // Now that we have instructed the computer to generate a random number between 1 and 6, next we will write the code which will display the corresponding image based on the number randomly generated by the computer.
  // {box:example}
  // If the computer generates a 1, the code will display dice 1.
  // {pic:Dice 1:5x5}
  // If the computer generates a 5, the code will display dice 5.
  // {pic:Dice 5:5x5}
  // {/box}
  // For this, we will need to use an `if` statement
  // * tap on `[+]` to add a new line
  // * tap on `[if]`
  // * tap on `condition`
  // * tap on `[dice roll]`
  // * tap on `[=]` (equals symbol)
  // * type in **1**
  // Your code should look like this:
  if $dice_roll = 1 then {
    skip;
  }
  // * tap on `do nothing` (in-between the **if** and **else** statements)
  // Here we will write the code which displays the artwork for dice 1.
  // * tap on `[a?? art]`
  // The `a?? art` button lists all the images you have in your library.
  // * tap on `[dice 1]`
  // * tap on `[post to wall]`
  arta†’Dice_1a†’post_to_wall;
  // 
  // Now lets add the code for dice 2. We will add the next if statement directly after the else statement.)
  // * tap on `do nothing` (immediately after the last **else** statement)
  // * tap on `[if]`
  // * tap on `condition`
  // * tap on `[dice roll]`
  // * tap on `[=]` (equals symbol)
  // * type in **2**
  // * tap on `do nothing` (in-between the newly added  **if** and **else** statements)
  // Here we will write the code which displays the artwork for dice 1.
  // * tap on `[a?? art]`
  // The `a?? art` button lists all the images you have in your library.
  // * tap on `[dice 2]`
  // * tap on `[post to wall]`
  // Repeat the above steps for dice 3 to 5. Your code should look like this:
  if $dice_roll = 1 then {
    arta†’Dice_1a†’post_to_wall;
  }
  else { if $dice_roll = 2 then {
    arta†’Dice_2a†’post_to_wall;
  }
  else { if $dice_roll = 3 then {
    arta†’Dice_3a†’post_to_wall;
  }
  else { if $dice_roll = 4 then {
    arta†’Dice_4a†’post_to_wall;
  }
  else { if $dice_roll = 5 then {
    arta†’Dice_5a†’post_to_wall;
  }
  else { } } } } } 
  // Finally, we need to write the code which displays the artwork for dice 6.
  // As this is the last possible number we do not need to write another if statement. Instead, we will add the code for dice 6 directly after the last else statement.
  // * tap on `do nothing` (immediately after the last **else** statement)
  // * tap on `[a?? art]`
  // The `a?? art` button lists all the images you have in your library.
  // * tap on `[dice 6]`
  // * tap on `[post to wall]`
  // * tap the run `[a–·]` button to test your game
  // 
  // ### CHALLENGE!
  // **Challenge 1:** Add a second dice to your game and edit the script so that it adds the total of the two dice and displays the total on the screen.
  // **Challenge 2:** Using the dice game as an example, create a coin toss game that will randomly pick heads or tails.
}

var create_script_button : Picture {
  is\_resource = true;
  url = "https://az31353.vo.msecnd.net/pub/pyvcltyz";
  transient = true;
}

var @3D_Dice : Picture {
  is\_resource = true;
  url = "https://az31353.vo.msecnd.net/pub/iffxtxtq";
  transient = true;
}

var varboard : Picture {
  is\_resource = true;
  url = "https://az31353.vo.msecnd.net/pub/xkbiosyi";
  transient = true;
}

var board : Board {
  transient = true;
}

var addnewlineicon : Picture {
  is\_resource = true;
  url = "https://az31353.vo.msecnd.net/pub/jsdyuerm";
  transient = true;
}

var add_new_action_screenshot : Picture {
  is\_resource = true;
  url = "https://az31353.vo.msecnd.net/pub/fzoynles";
  transient = true;
}

var searchonlineart : Picture {
  is\_resource = true;
  url = "https://az31353.vo.msecnd.net/pub/loljsfvv";
  transient = true;
}

var searchdice : Picture {
  is\_resource = true;
  url = "https://az31353.vo.msecnd.net/pub/llczhpuy";
  transient = true;
}

var diceart : Picture {
  is\_resource = true;
  url = "https://az31353.vo.msecnd.net/pub/cnefbnak";
  transient = true;
}

var Dice_1 : Picture {
  is\_resource = true;
  url = "https://az31353.vo.msecnd.net/pub/zohkkcfr";
  transient = true;
}

var Dice_5 : Picture {
  is\_resource = true;
  url = "https://az31353.vo.msecnd.net/pub/gltqvofc";
  transient = true;
}

var Dice_2 : Picture {
  is\_resource = true;
  url = "https://az31353.vo.msecnd.net/pub/csvgrcog";
  transient = true;
}

var Dice_3 : Picture {
  is\_resource = true;
  url = "https://az31353.vo.msecnd.net/pub/rrrjehqq";
  transient = true;
}

var Dice_4 : Picture {
  is\_resource = true;
  url = "https://az31353.vo.msecnd.net/pub/mdapuxtp";
  transient = true;
}

var Dice_6 : Picture {
  is\_resource = true;
  url = "https://az31353.vo.msecnd.net/pub/szihnyqx";
  transient = true;
}
